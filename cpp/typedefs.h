#pragma once

#include <numbers>
#include <complex>

namespace ahr {
    using Real = double;
    using Dim = size_t;
    using Complex = std::complex<Real>;

    using namespace std::complex_literals;
}